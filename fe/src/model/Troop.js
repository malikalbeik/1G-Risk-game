class Troop {
    constructor(deployedCountry, color) {}

    setDeployedCountry() {};
    getDeployedCountry() {};
    
    setColor() {}; // Lets keep this so that in the future we can add more features.
    getColor() {};

};

export default Troop;